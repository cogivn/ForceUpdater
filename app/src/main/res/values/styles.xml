<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AlertDialogCustom" parent="Theme.ForceUpdateAlertDialog">
        <item name="shapeAppearanceOverlay">@style/AlertDialogCustom.Rounded</item>
        <item name="android:windowAnimationStyle">@style/AlertDialogAnim</item>
    </style>

    <style name="AlertDialogAnim" parent="@android:style/Animation.Activity">
        <item name="android:windowEnterAnimation">@anim/anim_modal_in</item>
        <item name="android:windowExitAnimation">@anim/anim_modal_out</item>
    </style>

    <style name="AlertDialogCustom.Rounded">
        <item name="cornerSize">20dp</item>
    </style>
</resources>